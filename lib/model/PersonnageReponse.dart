/*
import 'package:flutterapi/model/Personnage.dart';

class PersonnageResponse {
  final List<Personnage> list;

  PersonnageResponse({
    this.list,
  });

  factory PersonnageResponse.fromJson(List<dynamic> parsedJson) {
    List<Personnage> list = new List<Personnage>();
    list = parsedJson.map((i) => Personnage.fromJson(i)).toList();

    return new PersonnageResponse(list: list);
  }
}


 */